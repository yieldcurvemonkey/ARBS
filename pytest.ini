[pytest]
markers =
    integration: integration tests requiring live market data (deselect with '-m "not integration"')
    live: end-to-end tests requiring live market data (deselect with '-m "not live"')
