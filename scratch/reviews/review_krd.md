**DEFECT 1 — the module under review does not exist.**
`SDRUtils/dealer_direction/krd.py` — absent. Verified 12 times between 11:53:46 and 11:59:18 EDT (2026-08-11), by `ls` and by `os.path.exists` under the stir interpreter. The builder is in flight, not finished: `scratch/ddkrd_p1.py`…`p13.py` (11:27–11:51), `scratch/ddkrd_perday_*.csv` (11:44–11:45), `scratch/KRD_RESULTS.txt` (10:23). No module in the package imports it; the only reference is the string at `SDRUtils/dealer_direction/provenance.py:78` in `VINTAGE_SOURCES`, whose missing-file branch (`provenance.py:122-123`, `h.update(b"<missing>")`) is deliberate. **Concrete failure:** with no krd frame, `unit_ladder_rows` sends every unit down `ladder.py:245-249` and names it `EXCL_PRICING_ERROR` — so the loss is named, not silent. But the function returns `(rows, excluded)` and a caller that reads only `rows` gets an **empty ladder with no exception**: zero rows reported as success, the exact class-4 failure the mandate names. `lifecycle.py` is likewise absent (out of my scope, noted only because it is the other `VINTAGE_SOURCES` gap).

**DEFECT 2 — `tests/test_dealer_direction_krd.py` does not exist.** Defect class 2 ("tests that cannot fail") is trivially maximal: there is nothing to mutate. Review of classes 1, 3, 4, 5, 6 against the named files was **not performed** — this is an unreviewable module, not a clean one.

The task text refers to "the report below"; no report was included in my instructions.

---

**Pre-registered checks for whoever re-runs this after the builder lands.** These are hazards derived from files that *do* exist, not confirmed defects.

1. **The sign trap, and it is live.** The downstream contract is `ladder.py:78` (`KRD_VALUE_COL = "dv01_if_received"`) and `ladder.py:183-186`: the frame must carry *"the profile the dealer would hold IF the dealer received fixed"*. So the discriminating check is
 `dv01_if_received[i] == base_orientation(kind, n, rule)[i] * pv01[i]` with `pv01 > 0` and **`dealer_sign` forced to +1** — never `call.dealer_sign * o * pv01`.
 `DESIGN.md:201-203` says the profile is *"`dealer_sign * o` applied to the printed leg notionals"*. That describes the dealer's composed position, not the module boundary, but a builder implementing that sentence literally at the boundary produces the failure. **Failure signature:** `ladder.py:260` computes `delta_dv01 = (2p-1) * dv01`, and `_assert_weight_agrees_with_side` (`ladder.py:305-312`) *enforces* `sign(2p-1) == dealer_sign`. A dealer-signed input therefore cancels the direction bit entirely — `|2p-1| * |dv01|` — giving a ladder that is positive in every bucket on every day and no longer depends on the inferred direction at all. The `DoubleSignedKRD` guard at `ladder.py:174-182` catches this **only** if krd.py also ships a `dealer_sign` column; ship a signed value under the correct column name and no column, and `_validated_krd` returns it unchallenged.
 Harness, validated before use and ready to run: `C:\Users\chris\clee\ARBS-dd\scratch\ddrev_krd_signtrace.py` — 10/10 tie-out of `conventions.dealer_received_signs` against the frozen `SDRUtils/stir_flow/ladder_conventions.dealer_leg_signs` on every case that function supports, plus the pinned worked example (OUTRIGHT, pv01 = 900 USD/bp, customer pays fixed → dev +0.25bp → `dealer_side` = +1 → `dv01_if_received` = +900 → p = 0.80 → `signed_weight` = +0.6 → **delta_dv01 = +540**, and the mirror at p = 0.20 → −540). The sign chain in `conventions.py` is internally consistent and matches the frozen predecessor; the risk is entirely at krd.py's boundary.

2. **Route conflict with an explicit instruction.** `LEDGER.md` D9 and `DESIGN.md:265` (§6 Options rejected) both reject *"hand-rolled analytic KRD by cashflow bucketing"* — D9 marks it "user instruction, 2026-08-11", DESIGN says "instructed against"; the sanctioned route is rateslib `Solver` + `Portfolio.delta`. `scratch/KRD_RESULTS.txt` shows the builder benchmarked and validated exactly the rejected route (analytic node-tent derivative converted through the zero→par Jacobian `J⁻ᵀ`), with the apparent motive in its own measurements: 42.2 ms per curve snapshot × 547,338 distinct `(rate_index, exec-minute)` snapshots for the sanctioned route, versus 0.023–0.116 ms/trade analytic. Flagging the tension; not adjudicating it.

3. **Bucket-spine fidelity versus any accuracy claim.** `probability.py:233-243` defines a **13**-band spine and asserts it is *"the same spine as the KRD ladder"*. `KRD_RESULTS.txt` measured the `J⁻ᵀ`-converted analytic KRD against rateslib's solver par delta at **21 buckets** (worst 0.28% of total DV01) and at **15 buckets** (4.58% at 11Y, 5.14% at 1Yx10Y, **118% at 40Y**). A 13-band ship is coarser than the 15 that already fails. Check the shipped bucket count against whatever tolerance the docstrings claim, and confirm the same file's own conclusion is honored — *"USE THE NODE-INTERPOLATED TENT"* (the continuous tent was measured 0.378% off at 30Y where Citi node spacing jumps to 5y past 20y).

4. **Lookahead.** krd must take its curve from `Clocks.pricing` (`types.py:49`) via `snapshot.py`, never from `Clocks.execution` — field #96 is frozen at the original trade on any row that does not mint a new UTI and can be years stale. `UnitPricing.snapshot_lag_seconds` (`types.py:111`) must come back non-negative; negative means a curve from the future.

**Clean, one line each:** the sign chain in `conventions.py` is correct and pins to the frozen predecessor exactly (10/10). `provenance.annuity_dv01_proxy` (`provenance.py:216-229`), the only live DV01-producing code in this functional area today, returns an unsigned magnitude used for exclusion DV01 shares — it does not feed anything summed into the ladder and is not a stand-in for krd.